version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - DEBUG=False
      - ENVIRONMENT=production
    restart: unless-stopped
    volumes:
      - .:/app
